apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresql-helm.fullname" . }}-test-connection"
  labels:
    {{- include "postgresql-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresql-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
